import React from 'react'
import PropTypes from 'prop-types'

const Gradiant = props => {
    return (
        <div class="bg-gradient-to-r from-yellow-400 via-red-500 to-pink-500  h-40 w-40"></div>
    )
}

// Gradiant.propTypes = {

// }

export default Gradiant
